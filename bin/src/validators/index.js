const SchemaValidator = require('./schema');
const MaxValueValidator = require('./max-value');

//this file is here only to make exporting validators easier
module.exports = { SchemaValidator, MaxValueValidator };
